# skill-writer checklist - meta-dream-tree (2026-07-17, --check-tree is a deliverable, matching the procedure)

Change: Step 8 requires BOTH `--check` and `--check-tree` (the cross-sibling duplicate/orphan check the chain-only
--check structurally cannot make), but the Deliverables checklist named only the chain check - so a dream
could self-certify done while skipping it.

- [x] Receipt held (skill_receipt.py start meta-skill-writer, this session)
- [x] RED: read the deliverables against step 8: only 'TOTAL problems: 0' is listed; 'TOTAL tree problems: 0'
      is absent though the procedure calls it mandatory
- [x] GREEN: added the --check-tree deliverable naming what it catches
- [x] Verified against ground truth before editing (not taken from the review agent's report on faith)
- [x] CSO description: unchanged (body edit only)
- [x] Security scan: prose/doc change only, no secrets, hostnames, or private paths
- [x] Docs describe current state: no legacy/migration narrative introduced
