# skill-writer checklist - process-debug-systematic (2026-08-12, closed-source peer disassembly escalation)

- [x] Change: Phase 3 (Hypothesis and Testing) had no guidance for a closed-source/black-box peer - repeated hypothesis testing against measurement could continue indefinitely with no escalation to reading the code. Added step 5: after a second black-box hypothesis dies against measurement, stop and disassemble the peer (a disassembler such as Ghidra plus any public PDB/symbols the vendor ships), citing an address and a symbol for the conclusion, treating a clean refutation as an equally valid result, and reading the code only for interoperability (the protocol), never to copy the implementation. Prose-only; frontmatter description unchanged.
- [x] Receipt held (skill_receipt.py, this session)
- [x] RED (baseline-probe, sonnet, pre-change text): given a closed-source-peer scenario with two dead black-box hypotheses and one test slot left, chose to stop guessing and gather more evidence (verify the prior tests actually ran, add own instrumentation, ask the vendor for the spec) but did not reach for disassembling the peer itself - the technique this step adds was absent from the baseline response.
- [x] GREEN (baseline-probe, sonnet, new text): given the same scenario plus the new text, quoted the governing clause verbatim - "2nd hypothesis also dies against measurement -> STOP. Do not form a third black-box guess." - and named the specific escalation ("the skill's actual disassembly escalation (Ghidra + PDB)") as the action to work toward.
- [x] Skill gaps (RED): unsure of the pairing transport, whether the error code is specific, whether the overnight harness reports a failure rate, what capture tooling is on hand, and had no access to real logs/vendor contacts/site conditions.
- [x] Diff RED to GREEN: GREEN kept every RED action (verify prior tests, add instrumentation, ask vendor) and added the taught technique (disassembly via Ghidra/PDB) as the next step once evidence is in hand - no RED result was lost.
- [x] CSO / description: unchanged - `build_skill_docs.py --check` confirms `docs/skills.md` stays in sync; no rebuild needed.
- [x] Security scan: prose-only note; no secrets, private hostnames, absolute machine paths, or internal project codenames - the example scenario uses a generic warehouse-scanner/handheld-terminal fixture, not real hardware.
