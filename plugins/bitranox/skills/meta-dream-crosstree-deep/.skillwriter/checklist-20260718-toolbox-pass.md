# skill-writer checklist - meta-dream-crosstree-deep (2026-07-18, inherit the toolbox contribute pass)

Change: noted in step 5 that the inherited "steps 4-8 of meta-dream-crosstree" now include the
toolbox contribute pass (step 7b: propose contributing a broadly-useful local tool + a cross-session
recurring chore -> a new tool), so a deep run surfaces tool proposals rather than generating them
silently.

- [x] Receipt held (skill_receipt.py start meta-skill-writer, this session).
- [x] RED: -deep runs "steps 4-8 of crosstree exactly", so it inherits the new step 7b automatically -
      but without naming it in step 5, a deep run could execute the pass and never surface the
      proposals (the same failure the org-chart report line already guards against).
- [x] GREEN: step 5 names the inherited toolbox pass; crosstree's step 8 report (which -deep runs)
      already carries the toolbox-proposals clause, so tool proposals are reported.
- [x] Single-sourced: no mechanics restated - the pass lives in dream-core.md and crosstree step 7b;
      -deep only notes the inheritance.
- [x] Security scan: prose only.
- [x] CSO description: unchanged (body edit).
- [x] Token budget: one clause added to step 5.
