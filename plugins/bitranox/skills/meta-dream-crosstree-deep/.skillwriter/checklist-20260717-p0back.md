# skill-writer checklist - meta-dream-crosstree-deep (2026-07-17, exhaustive misplacement sweep)

Change: added step 3b - run crosstree's misplacement audit over EVERY tree, not only the trees this
run touched, and report rejected candidates alongside relocated ones.

- [x] Receipt held (skill_receipt.py start meta-skill-writer, this session)
- [x] RED: a wrong-tree fact is invisible from inside its own tree, so a convergence-gated routine
      dream (which skips unchanged stores) can never find it - only an exhaustive sweep can. The
      deep mode had no such pass; the gap was structural, not incidental.
- [x] GREEN: step 3b names the detector, the judging duty, and the exhaustive scope
- [x] Reporting: rejected candidates are a RESULT (the detector reports evidence, not proof), so
      the pass says to report them, not silently drop them
- [x] Single-sourced: the pass mechanics live in crosstree step 4b; -deep states only its delta
      (exhaustive scope), so the two cannot drift
- [x] Security scan: prose only
- [x] CSO description: unchanged (body edit only)
