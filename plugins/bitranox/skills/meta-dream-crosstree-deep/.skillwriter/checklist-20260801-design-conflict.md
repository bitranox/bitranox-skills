# skill-writer checklist - meta-dream-crosstree-deep (2026-08-01, design conflict resolved)

Source: a design conflict surfaced by the clean-room sweep and decided by the operator, one at a
time, on the verbatim text of both sides. Ships with plugin 5.133.0.

- [x] CONTRADICTION with its sibling on the SAME decision (the promotion gate, run inline on the
      main agent): this skill required "the `opus` tier", while `meta-dream-crosstree` requires
      "opus-class OR ABOVE (opus is the universally-available deep tier; fable sits above it but
      needs paid API credits)". Read literally, this skill told a session on a MORE capable tier to
      switch down or interrupt the user.
- [x] DECIDED: adopt the sibling's wording verbatim, including the parenthetical that explains why
      opus is the floor rather than merely asserting it. Verified after the edit that both skills
      carry the same rule and the same fable caveat.
- [x] Receipt held (`skill_receipt.py start meta-skill-writer`, re-issued this session).
- [x] Both sides were quoted verbatim to the operator before the choice; the decision is theirs and
      the reasoning is recorded here rather than inferred later from the diff.
- [x] No session narrative or private provenance added; no machine paths added.
