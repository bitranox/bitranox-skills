# skill-writer checklist - write-humanize-en (2026-07-14, capability check on AI-tell discrimination + rewrite)

- [x] Change: added a "Capability check" note at Your Task - detecting subtle AI-writing tells and rewriting for natural voice is capability-sensitive (research: only capable models catch these reliably), so on a lesser tier delegate the edit to a pinned `sonnet`/`opus` subagent or offer switch-model-or-continue. Cites `process-agents-subagent-driven-development`. Prose-only; description unchanged.
- [x] Receipt held (this session)
- [x] RED (audit): the discrimination/rewrite task ran inline with NO subagent/model/tier/switch-model language.
- [x] GREEN: same inline-judgment pattern as the receiving-code-review representative live GREEN (haiku declined to self-judge and offered pinned-subagent delegation / switch-model).
- [x] CSO / description: unchanged - no rebuild; in sync.
- [x] Security scan: prose-only note; no secrets/paths/PII. (Pre-existing em-dash/curly-quote SPECIMENS in this file are intentional teaching examples of the tells to remove - not introduced by this change; the file-level tell-sweep hook flags them but they must stay.)
