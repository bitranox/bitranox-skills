# skill-writer checklist - compuse-toolbox (cross-reference the `wtclean` tool)

Change: index a newly-shipped tool that lives in a DIFFERENT skill (`git-worktrees`) so this
skill's tool table stays the one place that answers "is there already a tool for this?". No script
is added here - see `git-worktrees`'s own checklist (`checklist-20260812-wtclean.md`) for the
RED/GREEN work on the tool itself.

## PLAN

- [x] Skill type: reference (tool index with a per-tool rationale). This edit adds a table row, a
      rationale bullet, and a description clause - the same three-part shape every other tool in
      this index uses - but no `scripts/`/`tests/` pair, because `wtclean` ships from
      `git-worktrees` (it finishes that skill's own procedure: the skill tells you to give each
      worktree its own build cache and never reclaims it).
- [x] Checked it is not already indexed: no existing row removes a worktree or reclaims disk.
      `git_state` is the nearest neighbour and answers a tracking-status question about files you
      already name, not "what is eating my disk". Confirmed by the RED retrieval run, where an
      agent shown the pre-change table answered NONE and explicitly declined `git_state`.
- [x] Scope: one table row, one rationale bullet, one description clause. No script, no test file,
      no new dependency on this skill's own gate.

## RED

- [x] Retrieval baseline (RED) run via `bitranox:baseline-probe` against the PRE-change table (no
      `wtclean` row), NONE explicitly allowed, in the user's own words about a deleted worktree
      and a still-full disk. The agent answered NONE, declined to force-fit, reached for
      `du`/`ncdu` and `rm`, and named the missing capability itself. (Full question text and both
      transcripts live in `git-worktrees`'s checklist, since the row and the retrieval test are
      one change split across two SKILL.md files.)

## GREEN

- [x] Retrieval run (GREEN) against the updated table with the SAME question and rules: the agent
      named `wtclean` on the first try, quoted the row's own wording back as the match, filled in
      the exact invocation, led with the dry-run form before `--apply`, reached for `--cache-dir`
      for a nonstandard layout, and correctly read the "(ships in `git-worktrees`, not here)"
      parenthetical as meaning the invoke path is relative to that other skill. No rewrite needed.
- [x] Row is written in the USER'S NOUN, not the mechanism: it opens on the hand-rolled
      `du | sort -rh` then `rm -rf` hunt and the deleted worktree that never gave the space back,
      rather than on plan objects or refusal predicates.
- [x] The Invoke column carries a REAL runnable value (`my-feature`), not a placeholder
      metavariable, matching the convention the other rows follow.
- [x] Description clause names the SYMPTOM (hunting with `du` and `rm -rf` for space a deleted
      worktree never gave back), not the implementation.

## REFACTOR

- [x] Cross-checked against the three most recent additions to this same table (`diffbehave`,
      `newest`, `redcheck`): same three-part shape (row + bullet + description clause) and the
      same "ships in `X`, not here" parenthetical style `redcheck` and `snortblock` established
      for a tool that ships from a different skill.

## Quality

- [x] Present tense, no session narrative, in the skill and in this artifact.
- [x] Frontmatter `description` stays a single-line plain YAML scalar, still trigger-first ("Use
      when..."), still yields well over 3 distinctive keywords after the added clause.
- [x] Table realigned by the repo's markdown-table hook after each edit; ASCII only.

## Deliverables

- [x] `SKILL.md` table row + rationale bullet + description clause (no script/tests - those ship
      in `git-worktrees`). `plugin.json` bump, `docs/skills.md` regeneration, and the
      `CHANGELOG.md` entry are shared with that skill's change and recorded once in this same
      release. `skill_triggers.json` regenerated byte-identical: `distill()` caps each skill at
      14 keywords and this description's opening clauses already fill that quota.
