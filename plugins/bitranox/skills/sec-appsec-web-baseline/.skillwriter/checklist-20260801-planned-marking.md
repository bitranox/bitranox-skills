# skill-writer checklist - sec-appsec-web-baseline (2026-08-01, mark reserved names PLANNED)

Change: the five scope-table handoffs to unbuilt siblings now read "(PLANNED, not yet shipped)".
Ships with plugin 5.132.0.

- [x] Receipt held (`skill_receipt.py start meta-skill-writer`, re-issued this session).
- [x] REVERSES my earlier disposition in this same session, on the operator's decision. I had
      declined these five under the standing 2026-07-05 rule that reserved roadmap names are left
      alone, then marked the identical pattern in `web-frontend-responsive-ux` - leaving the
      catalogue inconsistent. Both now use one convention.
- [x] The 2026-07-05 intent is preserved, not overridden: the rows still name the reserved slugs,
      a future sibling is still to be built under exactly that name, and nothing was deleted. What
      changed is that the reader can now tell a planned handoff from an installable one.
- [x] Side benefit worth the edit: a future audit will not re-report these as DANGLING, because the
      text now states they are unbuilt. That closes a recurring false positive rather than
      re-declining it every sweep.
- [x] No session narrative or private provenance added; no machine paths added.
