# skill-writer checklist - meta-dream-nap (2026-07-18, defer the toolbox pass)

Change: added "the toolbox pass per dream-core.md" to the nap's reported "deferred to the full dream"
list (step 6). The toolbox is machine-global, outside the nap's chain-only minutes budget, so the
nap explicitly DEFERS it rather than doing partial global work silently.

- [x] Receipt held (skill_receipt.py start meta-skill-writer, this session).
- [x] RED: without this the nap would either silently ignore the toolbox (leaving a gap the user
      cannot see) or overreach into machine-global work it is not scoped for.
- [x] GREEN: the toolbox pass is named in the deferred list, matching nap's "partial by design, say
      so" contract; the real pass runs in the tree/crosstree dream (dream-core "Toolbox pass" nap delta).
- [x] Single-sourced: cites dream-core.md; no mechanics restated.
- [x] Security scan: prose only.
- [x] CSO description: unchanged (body edit).
- [x] Token budget: one clause added to an existing list.
