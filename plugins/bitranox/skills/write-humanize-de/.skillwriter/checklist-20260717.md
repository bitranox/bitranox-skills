# skill-writer checklist - write-humanize-de (2026-07-17, both procedural lists run the mandatory script; tell examples preserved in code spans)

Change: Same defect as its English twin (fix-shared-bug-in-all-siblings): the mandatory 'zuerst ausfuehren'
script pass appeared in neither 'Deine Aufgabe' nor 'Ablauf' - and Ablauf is the last thing read before
Output, so it reads as authoritative. The tell-sweep also blocked the file on prose tells.

- [x] Receipt held (skill_receipt.py start meta-skill-writer, this session)
- [x] RED: read both lists against the 'zuerst ausfuehren' section - neither names the script, exactly as in
      write-humanize-en, so the pair shared one defect
- [x] GREEN: the script run is now step 1 of both lists; prose tells fixed to ASCII; teaching examples moved
      into code spans/fenced blocks; the code-span rationale documented, mirroring the EN twin
- [x] Verified against ground truth before editing (not taken from the review agent's report on faith)
- [x] CSO description: unchanged (body edit only)
- [x] Security scan: prose/doc change only, no secrets, hostnames, or private paths
- [x] Docs describe current state: no legacy/migration narrative introduced
