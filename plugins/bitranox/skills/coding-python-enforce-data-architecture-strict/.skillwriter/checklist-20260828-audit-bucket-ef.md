# skill-writer checklist - coding-python-enforce-data-architecture-strict (2026-08-28, audit bucket E+F)

One unanchored claim: the rich-click pyright diagnostic the worked example exists to fix
named no version of either tool.

## PLAN
- [x] Receipt issued (`skill_receipt.py start meta-skill-writer`).
- [x] Skill type: reference/technique. The defect is a FACTUAL claim carrying no version or date,
      so the test is a ground-truth check against the installed package, the live catalogue or the
      running tool - not a pressure scenario.
- [x] Scope: correction only. No new capability, no procedure reshaped.

## RED
- [x] Behavioural RED deliberately NOT used: this skill is INSTALLED on this machine, so a probe
      answers from the shipped wording rather than the draft and cannot fail honestly. The route
      taken instead is a ground-truth check, whose result is immune to inherited context.
- [x] The `reportUnknownMemberType` diagnostic is a property of rich-click's type STUBS at a
      given version, not a language rule, and the passage cited neither rich-click nor pyright.
      A reader on a later rich-click could apply the Protocol-cast facade to a problem that no
      longer exists, or be confused when pyright reports nothing to fix.

## GREEN
- [x] The claim now names rich-click 1.9.8 and pyright 1.1.411 (the current PyPI release and the
      installed checker, both read live) and tells the reader to run pyright FIRST, so a
      completed stub set shows up as "nothing to fix" rather than as confusion.

## Quality
- [x] Present tense; no session narrative, no operator instructions, no scratch paths.
- [x] No address, MAC, hostname or machine path added.
- [x] Frontmatter untouched, so no routing keyword moved and the description cap is unaffected.
