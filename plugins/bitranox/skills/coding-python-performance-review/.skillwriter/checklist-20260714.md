# skill-writer checklist - coding-python-performance-review (2026-07-14, re-assess accepted/declined items vs ground truth)

- [x] Change: mirrored the process-review-enhance-code-quality reframe (fix-shared-bug-in-all-siblings - this skill carried the identical "OFF-LIMITS / skip them silently / Never re-raise" language). Respect previously-reviewed items by default but RE-ASSESS a declined finding against ground truth (an already-implemented fix stays done); a rotted decline becomes a propose-first "Reconsider" finding; added the Still-accepted summary and delegation of the respect-or-reconsider judgment to a pinned `sonnet` subagent. Touched: Step 1, Step 5, Step 6 (Reconsider format + summary), Common Mistakes, Key Behaviors.
- [x] Receipt held (skill_receipt.py start meta-skill-writer, this session)
- [x] RED baseline: the identical "OFF-LIMITS / skip silently" mechanism makes a literal follower silently skip an accepted item whose premise rotted - demonstrated by the enhance-skill haiku RED ("New issues found: None", missing a live OOM); this skill shipped the exact same language, so the same failure applies.
- [x] GREEN: haiku (literal follower) under the edited sibling skill produced `## Reconsider accepted item 1: No streaming in export_report()` (Originally accepted / What changed (ground truth) / Proposal) for a declined "no streaming" item now pointed at a ~4 GB continuously-appended log (SEVERE OOM on a nightly cron), propose-first, plus a Still-accepted summary respecting the unchanged trivial-function decline.
- [x] CSO / description: frontmatter description UNCHANGED - no `build_skill_triggers.py` rebuild needed; verified in sync.
- [x] Security scan: prose-only edits; no secrets, credentials, private hosts/paths, or PII; example project generic (reportsvc).
