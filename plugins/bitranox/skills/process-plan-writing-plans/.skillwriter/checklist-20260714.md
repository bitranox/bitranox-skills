# skill-writer checklist - process-plan-writing-plans (2026-07-14, capability check on decomposition + self-review)

- [x] Change: added a "Capability check" note at the File Structure / design-units step - decomposing a spec into well-bounded units and self-reviewing the plan is capability-sensitive, so on a lesser tier delegate the design/decomposition (or a critical self-review pass) to a pinned `sonnet`/`opus` subagent or offer switch-model-or-continue. Cites `process-agents-subagent-driven-development`. Prose-only; description unchanged.
- [x] Receipt held (this session)
- [x] RED (audit): the decomposition and the "self-review" checklist ran inline with NO subagent/model/tier/switch-model language (the self-review is explicitly "not a subagent dispatch").
- [x] GREEN: same inline-judgment pattern as the receiving-code-review representative live GREEN (haiku declined to self-judge and offered pinned-subagent delegation / switch-model).
- [x] CSO / description: unchanged - no rebuild; in sync.
- [x] Security scan: prose-only note; no secrets/paths/PII.
