# Open work

The standing backlog: everything that outlives a single session. `handover.md` describes one
moment and is replaced wholesale every time; this file is not. Items leave it by being CLOSED,
never by being dropped.

One item per line, in this shape:

```
- [ ] (YYYY-MM-DD) [rank] ORIGIN: what it is | size: how much is left | open: why it is still open | next: the concrete next action
```

- `(YYYY-MM-DD)` is when it was FIRST raised, and it never changes. Age is the point. A date
  nobody recorded is written as today's with a `?`, or as `(unknown)`; both parse, and an
  undated item sorts last within its rank. Never guess one.
- `[rank]` is priority, lowest number first, in TENS so an insertion is a new number rather than a
  renumbering of everything below it. `USER:` items outrank `FOUND:` items; a `USER:` item the
  user deferred sits below the live ones and still above every `FOUND:` one; within an origin the
  bigger `size` goes first. Blocked keeps its rank.
- `ORIGIN` is `USER:` or `FOUND:`. There are two. Blocked is a value for `open:`, not an origin.
- `size` is the count that says how big it is. It is the first field that gets dropped when this
  is retyped from memory, so it is not optional; `size: unknown` is honest, an invented count is
  not.
- Closing an item means `- [x]` plus `| closed: <reason>`. The line stays.

SessionStart prints the top ranks with their age. It does not consume them.

- [x] (2026-09-22) [5] USER: fix the skill router's input, then feed Jev the notification's own fields - "3,4 as the first thing to do after the handover" | size: 2 changes in skill-router.py (skip machine turns, strip non-prose before matching) plus tests, then a shadow-only field change for notifications | closed: 2026-09-22 in 7.6.0. hooks/prompt_text.py (new) answers both questions and the router AND recall ask it: a machine turn scores nothing, and tag markup, ids, hashes and dashed slugs are removed while a path is reduced to its file TYPE. Ground truth from the router's own log first: 16 of 40 rows were notifications and EVERY one fired, 9-11 skills over threshold. Replayed over 5,354 corpus records, firings fell 2,403 (44.9%) to 1,353 (25.3%), 1,043 of the 1,050 removed being machine turns; of the 8 typed prompts that lost their only firing when paths were dropped whole, 7 were path noise and 1 was genuine (a defaultconfig.toml request losing files-edit-toml), which the extension rule recovers while admitting nothing - keeping the basename instead recovered it plus 2 false firings, so it was rejected on the measurement. Option 4: a notification now reaches the shadow as task_status/task_summary with its own gate question, tagged notify_view: fields-v1; its output file is deliberately not read, since a forged envelope could then name any file on the machine | open: raised 2026-09-22 from the 7.5.0 shadow rows. Measured: a 409-char `<task-notification>` envelope reached the 2-keyword threshold for ELEVEN skills, because the matcher scores the output-file PATH - `claude`, `code` and `bitranox` all came from /tmp/claude-1000/-media-srv-main-softdev-bitranox-systems-... - and each firing spends that skill's once-per-session nudge, so the prompt where it would have been right stays silent. 15 of 34 router rows in one log were notifications. recall-memory.py has the same input. Detail: memory fact feedback-a-keyword-matcher-scores-paths-and-envelopes-not-just-prose | next: option 3 first - skip prompts starting with the NOT_TYPED_PREFIXES shapes AND strip tags, paths, ids and hashes before `match()`, with a test per class and a replay over transcript prompts to check the firing rate falls without losing real matches; then option 4 - send Jev a notification's `status` and `summary` (and possibly a tail of its output file) as their own fields instead of as `user_prompt`, shadow only

- [ ] (2026-08-27) [10] USER: "review all skills and scripts one by one, each in its own subagent and ask when smth is to change" | size: 106 adjudicated hook-lib findings to fix in 7 batches (A-G), 84 targets still unswept (79 skill-script, 2 js, 1 shim, 2 hooks added since 2026-08-28), plus the old buckets - 5 untested-but-correct coverage gaps, about 7 reports named only in passing, 17 claims left unadjudicated inside the guard slice | open: the 7 gate/guard reports were adjudicated 2026-09-01 (5 under-blocks shipped in 5.299.0) and the 6 non-guard reports were adjudicated 2026-09-01 - 22 claims driven, 15 confirmed, 6 already fixed, 1 split - and the 2 session-start decoy-check findings shipped in 5.299.2 (48b616b), the scope the user chose. The 8 remaining confirmed behavioural findings shipped in 6.12.0 on 2026-09-12 on the user's word ("fix that 8 findings"); re-driven against 6.9.1 first, the Bash-only prose finding was 36 stale claims rather than 3, and the brief's clean-room exclusion turned out to miss every NAMED probe. An adversarial review of those fixes found nine more, fixed in the same release; replaying one of them through the real audit over 250 transcripts found six more, and a seventh (a hook registered for Monitor that ignored it) was found by the registration test's first run against master 2026-09-25: the old TRIAGE.md and its 53 reports are rescued (byte-verified) to the main checkout's .plan/rank10-review-2026-08-28/. The user chose "sweep the never-reviewed scripts" over the old buckets; slice 1, the 22 hook-libs, was swept (recall walled and restored) and every one of its 106 findings adjudicated by five opus agents against the live tree with controls: 0 refuted, about 95 confirmed, 11 partial, 21 new. Three HIGH batches: secret_patterns misses JSON/YAML/docker -e passwords, sk-proj- and gho_ keys and PGP blocks on live classifier egress and repo-gate; shell_text lets blocking guards silently allow after a here-string or a "# don't" comment; memory_engine relocate can overwrite a body cross-tree and add --slug accepts ../. Record: .plan/rank10-hooklib-2026-09-25/ (TRIAGE.md with batches A-G, reports/, adjudication/) | next: ask the user which fix batch goes first (recommended A, secret redaction), fix it TDD in its own worktree and release, then the next batch; sweep the skill-script slice after the HIGH batches
- [ ] (2026-09-21) [12] USER: Jev (TypeSafe classifier) as an opt-in experiment beside the existing hooks, "not throw away existing, working code", and "discover where else it would be beneficial" | size: rest of step 5 (corpus replay with planted controls, the two variants, results doc), step 6 (decide mode per site, ie whether any site leaves shadow), second wave of 6 sites | open: all three sites log in shadow mode. 7.2.0 added `classifier_eval.py report`; 7.3.0 shows recall each note as `name: description` (not the keyword-centred window); 7.4.0 adds `previous_assistant_message` (the reply the prompt answers, ~300 chars, both ends) to all three sites, ignoring an answer to Stop-hook feedback. 7.5.0 (user's choice of options 1-4, 2026-09-22) gives the router a `_new_task` gate question plus `project`, `recent_activity` and `skills_already_used`, because on 12 rows of 7.4.0 every router pick was a generic pair (zpools for "check the shadow stat"); it costs ~14k tokens per prompt against 11.5k. On those 12 rows recall's "shadow" confusion was gone (0.46) but short prompts still drew unrelated notes at ~0.8, and `endorsement` fired 0.94 on a plain "yes" to "write the handover?". 7.7.0 settled two of this item's open questions on the 124-row log: `endorsement` is logged but no longer counts as a Stop-gate firing (12 of 12 firings across two windows were plain approvals, which moved 7 rows from jev_only to neither, so the gate's 25 apparent misses are 18), and each site is judged at its OWN threshold (stop_signal 0.7, skill_router 0.7, recall_rerank 0.8 - over 1,177 pair judgements 0.5 keeps 20% at about 5.9 notes a prompt, 0.8 keeps 4% at about 1.1). Still open here: whether any site leaves shadow, which is the one question the shadow log cannot answer by itself. Cost to weigh when deciding: recall 17,724 tokens per prompt (one request per shortlisted note), router 13,664, stop gate 1,069. Rows are grouped by their `*_view` tags, so older rows do not pool with 7.5.0 router rows. 7.6.0 (rank 5) fixed the input both the router and recall score, so from here on the keyword baseline in a notification row is correctly empty and those rows carry `notify_view: fields-v1`; rows logged before it had 40% of the router's traffic judged on an envelope. MEASURED on 81 rows 2026-09-22 (7.5.0 loaded): the router's gate works - 21 prompts produced 13 Jev picks against 1.8 per prompt before, every continuation ("go" 0.09, "continue" 0.11, notifications 0.09-0.39) suggested nothing, and real new tasks scored 0.71-0.85 with correct picks (the XML questions got coding-python-use-modern-libraries and files-edit-xml, a copilot review got process-test-design and process-review-receiving-code-review); Jev and the keywords agreed on 0 of 21, Jev usually the better one. Stop gate over 22 turns: regex fired twice, Jev alone 15 times at 0.7 - of those, the 10 `self_admission`/`realization` firings were real misses (corrections of earlier claims, root causes) and all 5 `endorsement`-only firings were noise on a plain "yes"/"go". Recall at 0.5 called 165 of 99 injected notes relevant over 28 prompts with a wide spread (p50 0.73), so it needs about 0.8 Measured before 7.4.0: recall's relevant-per-prompt fell from ~10 to ~5 with 7.3.0 and its top picks became sensible, but words like "shadow" still matched on descriptions; the Stop gate's 3 Jev-only fires in the 7.3.0 rows were REAL lessons the regex missed ("worth knowing ... review timing varies", a user's standing decision); the router still suggests the same generic pairs (debug + verification, context-watcher). `realization` over-fires on plain status replies at 0.5, clean at 0.9 on the 7.2.0 rows MEASURED 2026-09-23 in 7.10.0, the first run of the replay instrument (50 prompts, 25 continuations and 25 substantial, every arm on the SAME prompts): the shipping noul arm spoke on 4 of 50, one choice over the whole roster on 10, the same with shortened descriptions on 9, and the two-request rerank on 1. Re-thresholded offline from the recorded scores, choice_short is FLAT at 9 across 0.3 to 0.7 while nouls swings 38 down to 2, so the shape question is not only about cost: the noul arm is decided by a threshold and the choice arm is not. Cost per prompt, questions only: nouls 15,120 tokens, choice_full 9,066, choice_short 3,169, rerank 4,020. The rerank collapse was a THRESHOLD ERROR, not the shape: its per-candidate noul was judged at 0.7 where the recipe uses 0.30; at 0.30 it gives 5, and it still does not pay for itself because it suppresses some wrong picks and kills some right ones. Regex and Jev agreed on 0 of 50, again. NOT established: whether the picks are RIGHT - 9 rows adjudicated by the author of the change is a shape, not a proportion. NEW DEFECT found by a planted control: the `_new_task` gate question names `previous_assistant_message` and `recent_activity`, and `_router_fields` omits a field that is empty, so on the FIRST prompt of a session the gate is asked about context that is not there and hedges at 0.66-0.70 against a 0.7 threshold rather than answering. With those fields present the same prompt scores 0.89-0.90 and its negative 0.05-0.06. So the gate is a coin flip exactly where a session starts. MEASURED 2026-09-23, the ACCURACY question, which no arm had answered: all 50 replayed prompts adjudicated blind by five judges who saw the prompt, its state and a POOLED candidate list and never which arm named what - every pick classified, none sampled. 12 of 50 turns needed a skill, 11 named one. At the shipping gate of 0.7, right/defensible/wrong/missed: the KEYWORD matcher 0/1/4, missing 10 of 11; nouls 2/4/1, missing 8; choice_full 3/7/1, missing 6; choice_short 2/6/1, missing 7; the rerank arm 0/1/0, missing 11. So choice_full is the best arm on accuracy as well as on cost, the keyword matcher it would replace is the worst thing measured here, and the rerank is SETTLED - re-thresholded to the 0.30 its own recipe uses it names 5 picks while suppressing 5 of the wide pass's, and it does not pay for itself. The dominant failure is one systematic blind spot, not scatter: 6 of the 11 misses wanted meta-context-watcher for handover and backlog prompts (write handover, refresh the handover, tell me what is open), and the noul arm RANKS it top at 0.58-0.73 - the knowledge is there and both thresholds sit above it. CAVEAT on every offline sweep: a gated row recorded no choice answer before 7.11.0, so a sweep could only remove picks, never restore a suppressed one; 7.11.0 records it, and the earlier "choice_short is threshold-insensitive" reading is partly that artifact. MEASURED 2026-09-23, the COST premise that had been driving the arm choice, and it is a rounding error: at $0.042 per million input tokens (corroborated independently by the blocks.ai Jev replica post, "$42 per billion"), the whole spread is nouls $6.19, choice_full $3.81, choice_short $1.33, rerank $1.68 and the new choice_body $6.43 per TEN THOUSAND prompts. Latency is near-flat in tokens too: two single-request sites measured 1,121 tok / 809 ms against 14,141 tok / 1,276 ms, so the marginal rate is about 36 ms per 1k against a 15 s shadow deadline. So choice_short's 120-char cut was bought for about $2.50 per 10,000 prompts, and the arm should be chosen on ACCURACY alone. NO PREFIX CACHE: an interleaved 3-arm, 18-call probe found the reordered body order no faster than the shipping one and the broken-prefix control indistinguishable from both, with 9,400 input tokens charged on every call; usage carries only input_tokens and output_tokens, so a hit could not be reported. Excludes the replica's 5-6x, not a subtle effect. FULL skill bodies are out on size, not on caching: 1,469,460 chars over 81 skills is ~367k tokens a prompt and $154 per 10,000, and compuse-toolbox alone is 104,951 chars. 7.12.0 adds choice_body (700-char bodies, the cap load_skill_bodies already applies). VERIFIED 2026-09-23 against the live docs, after the state-vs-questions placement was questioned: the option catalogue belongs in a question's `criteria`, NOT in `state` (docs.typesafe.ai/concepts/state - "State is the content you ask a System One model to evaluate ... the material you would present to a panel of experts"; every worked routing example puts candidates in criteria; nothing documents state as cached or persisted). Correct, but it had never been a DECISION - a general rule applied once and carried through every arm. Choice takes up to 255 options, so widening to all 109 installed skills is within budget. The same read found three documented practices we did not follow: a one-line description per option (ours are keyword-matcher trigger lists), the OBJECT form {what, not_for, examples} for options the model keeps confusing, and "give the model the full list rather than a shortlist" - which independently condemns the rerank shape our own measurement already scored worst. 7.13.0 acts on the first two: hooks/router_criteria.json plus the choice_router_text arm, six structured entries for the families the adjudication named, everything else falling back to its description.  THE GRADER IS ON DISK AND NOT DURABLE: the five judges' blind labels (labels.json, all 50 prompts) and the scored replay logs carrying per-arm scores (replay-run2.jsonl) are at /tmp/claude-1000/-media-srv-main-softdev-projects-public-KI-bitranox-skills--claude-worktrees-jev-classifier/45b97af6-13f7-4877-8a62-0b1cf1242f33/scratchpad - copy them somewhere durable before that tmp tree is swept, because re-judging costs five opus agents and re-running the replay costs a paid run. MEASURED 2026-09-24, the GATE, which was the blocker: the rescued replay-run2 predates 7.11.0 so every gated row recorded NO pick, making the offline downward sweep the previous next: asked for structurally impossible - but the gate SCORE is on every row, and that answered it. Against the panel-1 labels the gate's AUC is about 0.71 on all four arms (needs-a-skill p50 0.61 against 0.52 for the rest, ranges almost fully overlapping), so no threshold separates cleanly and 0.7 sat in the steep part: it kept 5 of 11, reproducing the adjudication's 'missing 6' exactly. So the gate, NOT the roster, was the dominant failure, and the first-prompt control's 0.02 tripwire margin was a symptom of that, not a wording defect - `controls --threshold 0.5` PASSES all 24 rows on all six arms (positives 0.68-0.72, negatives 0.20-0.23). classifier_eval gained `--prompts LOG` (7.14.0) to pin a replay to an earlier run's exact prompts AND state: rebuilding state from source+line was tried first and is unfaithful (reproduced `project` on 39 of 50, differed on 8 because a dir had since gained its own scope descriptor, 3 cwds unresolvable), so a pinned run re-sends the RECORDED state and reports `state_drift`, which was 0 of 50 on the run below. One paid run now covers every threshold offline, because 7.11.0 records winner/probabilities/scores BEFORE the gate: replay-run4, 50 prompts, 2.82M input tokens, 331 requests, about $0.12. Re-adjudicated blind by five opus judges over the pooled candidates of all six arms at all thresholds (93 verdicts, 0 unjudged); 19 candidates overlapped panel 1 and 16 agree (84%), the 3 splits all one step (defensible<->right), never right<->wrong. right/defensible/wrong/missed of 13 needing a skill: choice_full 0.70 = 2/7/1/8 and 0.30 = 4/15/1/5; choice_router_text 0.70 = 4/6/1/8, 0.50 = 6/9/1/5, 0.30 = 9/12/1/2; choice_short and _rerank flat at 2 right; nouls is OUT - 42 outright WRONG at 0.30, because one number gates the turn and sets its per-skill bar. Every arm improves as the gate drops, so that is a property of the GATE and generalises. SITE_THRESHOLDS['skill_router'] is therefore 0.7 -> 0.5 (0.5 not 0.3 because 0.5 is where controls were actually run, clearing both ways by about 0.2; 0.3 leaves 0.07 over the negatives and has never been run). NOT ESTABLISHED and do not repeat it as a result: choice_router_text's lead is CONTAMINATED - 7 of its 9 right picks at 0.30 fall inside the six entries in router_criteria.json, which 7.13.0 authored FROM the panel-1 adjudication over these same 50 prompts; outside those six it scores 2 right, the same as choice_full. Also a separate ROSTER gap no threshold can close: 4 of the 13 wanted a skill NO arm proposed at any threshold (p02 meta-self-improve, p05 meta-context-watcher, p28 typesafe-ai, p43 soundtouch-decloud). Durable inputs: .plan/jev-replay-2026-09-23/ (panel-1 labels.json, replay-run2) and .plan/jev-replay-2026-09-24/ (replay-run4, labels2.json, verdicts2_0-4, sweep.py, check_fit.py), both byte-verified copies. SETTLED 2026-09-24 by a HELD-OUT run, and the contamination was real: 50 fresh prompts (25/25 stratified, seed 7) sharing ZERO prompt text with the first 50 - built by EXCLUDING those texts, because the corpus is only 1,177 typed prompts and the continuation stratum repeats ('go ahead'), so every seed tried still overlapped on 13-23 rows. Replayed at 0.5, drift 0, re-adjudicated blind by five opus judges (81 verdicts, 0 unjudged; 11 of 50 need a skill). choice_router_text does NOT beat choice_full off its own training prompts: at 0.30 both score 7 right / 3 wrong / 1 missed, and the inside-vs-outside split of their right picks is IDENTICAL at 4/3, where on the authored 50 it had been 9 right against 4. The only surviving difference is that router_text is chattier (13 defensible against 7), which is not an advantage. So prefer choice_full: equal accuracy, equal cost (36,245 chars a prompt against 36,112), and no hand-authored router_criteria.json to keep in sync with the skills it describes. What DID replicate on fresh prompts is the gate finding - every arm improves as the gate drops (choice_full 0.70 = 4 right/5 missed, 0.30 = 7 right/1 missed) - which is independent confirmation of 7.14.0's 0.7 -> 0.5. Roster gaps recurred: 2 of 11 wanted a skill NO arm proposed (provmm-build, update-config). ALSO MEASURED and REJECTED the same day: passing the previous turn's REASONING as a state field. It is structurally invisible today (a thinking block carries {signature, thinking, type} and text_of keeps only blocks with a 'text' key) and scarce - 1 of 46 surviving prompts in the first 50, and 12% of prompts even inside thinking-bearing sessions, of which only 11-16% of recent sessions are. Tested properly on a TARGETED stratum of 49 prompts that DO have it, each replayed twice adjacent so the arms interleave: it made the gate LOWER on 139 of 245 choice-arm cells against higher on 61 (mean and median -0.010) and the arms SPOKE LESS (56 -> 53 cells), the wrong direction when missing a needed skill is the dominant failure. Reverted; the rejection is pinned by a test and the numbers are in classifier._describing's docstring. Durable: .plan/jev-heldout-2026-09-24/ (held-out run, its labels, the reasoning A/B and every script). | next: the arm question is CLOSED - choice_full at gate 0.5. What remains is (a) decide per site whether anything leaves shadow, which no shadow log can answer by itself, and (b) the roster gap, which recurred on held-out data and is a recall/coverage problem, not a gate one: 2 of 11 here and 4 of 13 before wanted a skill no arm ever proposed. Reasoning was the candidate input for (b) and is now ruled out. (b) SHIPPED in 7.16.0: most of the gap was structural - the router's options came from globbing this plugin's own skills dir, so 4 of the 6 never-proposed skills (typesafe-ai, soundtouch-decloud, provmm-build, update-config) could never be offered. hooks/skill_roster.py now offers the session's installed skills (transcript skill_listing, else a per-project cache, else the glob). MEASURED on the held-out 50, pinned, choice_full only, `--roster installed`: at gate 0.5, of 11 needing a skill, installed 8 right/1 defensible/1 wrong/1 missed against shipped 5/3/1/2, noise on no-skill prompts 4 against 5; it leads at 0.3 and 0.7 too. Two unjudged picks went to five blind opus judges, unanimous (p28 provmm-pr right; p37 claude-api wrong, update-config right). Durable: .plan/jev-roster-2026-09-24/ (roster-run.jsonl, panel3.json, score_roster.py). Remaining roster-side misses are in-roster ones: meta-context-watcher on handover/backlog prompts, and p37 where claude-api's `[1m]` trigger beat update-config 0.68 to 0.19. 2026-09-24, for (a): the recommendation (skill_router to decide mode with choice_full at gate 0.5, keyword fallback on timeout/error) was put to the user, who chose "widen roster first" (done, 7.16.0), then "fix context-watcher first". On the first 50 at gate 0.5 choice_full caught 2 of 7 context-watcher turns, failing two ways: the GATE vetoing a confident choice ("refresh the handover" p(cw) 0.83 at gate 0.21) and the CHOICE answering none_needed ("what next", "list of open points"). The user chose the confidence bypass; 7.17.0 ships `classifier.choice_pick` (gate passes OR winner probability >= 0.7). MEASURED on a fresh 30-prompt handover/backlog stratum (excluding both earlier sets' prompt texts, five blind judges, 26 need a skill): gate only 8 right/1 def/0 wrong/17 missed, bypass 14/1/0/11, no noise either way; offline on the two labelled sets 0.7 added no no-skill pick. Durable: .plan/jev-roster-2026-09-24/ (cw-stratum.jsonl, cw-base.jsonl, cw-labels.json, bypass_sweep.py, harvest_cw.py). The none_needed half is SHIPPED in 7.18.0 as a DESCRIPTION change, not option text: the root cause was that meta-context-watcher's description named only WRITING a handover while its body covers READING one, so "read the handover and list the open points" had no option claiming it (a haiku router on the old wording said so verbatim: "None of the available skills cover reading files and reporting project status"). classifier_eval gained `--description SKILL=FILE`. MEASURED, old vs new wording, alternating runs: fresh 40-prompt handover/backlog set (excluding all three earlier sets' texts; five blind judges shown a NEUTRAL body summary, never either wording) caught 11 -> 21 of 32, identical in two runs, wrong 1 -> 0, noise 1 -> 1; native haiku routing 0/8 -> 8/8 twice. On the two older labelled 50-sets the new wording at first looked noisier (3 -> 7, 8 -> 13 picks on no-skill rows), all of them context-watcher on "read the handover" turns whose panels had judged with the OLD description visible; a fresh blind panel re-judged those 11 rows unanimous RIGHT and 4 unrelated controls 0 times, so relabelled there are 0 wrong context-watcher picks on any set. Durable: .plan/jev-cw-desc-2026-09-24/ (cw2-stratum, ab-* runs, cw2-labels, rejudge-labels, every script). STILL MISSED: "what next for <project>" turns (p(cw) under 0.1; the panel calls context-watcher right there) - a further wording needs prompts it was not written from, the cw2 set is now read. Then the router question is next again. NOTE before flipping: the live shadow hook still asks the NOULS arm (`classifier.skill_router_questions`); the replay-winning choice_full shape (`skill_router_choice_questions`) is not yet what the hook sends, so decide mode must switch the question shape too. Still queued behind it: stop_signal to one blind adjudication of its Jev-only firings and then union-decide, recall_rerank off (costliest at ~17.7k tokens a prompt, accuracy never judged). Ask them ONE at a time, the router first. Also settled that day: Jev has no role for the compuse-toolbox jigs - the router's option list holds one entry per skill, so the jigs share the single compuse-toolbox option, and a recall replay of the command nudge found its misses were regex gaps (fixed in 7.15.1), not a problem that needs a model. 2026-09-24 evening: asked decide mode vs the "what next" misses, the user chose the misses (B), and first a logging fix. 7.19.0 ships it: every shadow row carries plugin_version, transcript_path and transcript_offset, a failed comparison leaves an error row, and classifier_eval.locate_prompt(row) joins a live row to its prompt uuid (checked on real transcript bytes, both sides of the prompt plus a negative control). Only rows from sessions on 7.19.0+ carry the stamp. B SHIPPED in 7.20.0: context-watcher's description names "what next" / "what's next for <project>", pre-registered and passing all three criteria - on 10 fresh what-next prompts (every earlier set's texts excluded, five blind judges on a neutral body summary) caught 2 and 3 -> 9 and 9, no new wrong pick on four guard sets, and the 8 extra guard-set picks re-judged blind 6 right / 2 defensible / 0 wrong with 3 of 3 controls wrong. One reproducible loss, accepted as a like-for-like trade: "whats open ?" keeps cw as winner at 0.55-0.69 (was 0.78-0.88), under the 0.7 bypass, 3 of 3 reps. Native haiku routing was near ceiling already (18/20 -> 20/20). Durable: .plan/jev-whatnext-2026-09-24/ (PREREG.md, wn set, ab-* runs incl. the re-runs, wn-labels, rejudge-labels, native-wn-*, every script). 2026-09-24 late: decide mode for skill_router was put to the user, who chose "fix the shape, stay in shadow". 7.21.0 switches the live hook to the choice shape (rows tagged `question_view: choice-v1`) and `classifier_eval.py report` reads those rows through `choice_pick`. 7.22.0 bounds the shadow log (day files, 30 days / 200 MB) and `report` reads the whole audit dir. 2026-09-25, stop_signal ADJUDICATED blind (pre-registered, five opus judges over ALL 158 live turns, 92% unanimous, 51 signals): the regex fires 10 at 90% precision but catches only 9 of 51 signals (18%), and its firings are a subset of Jev's; Jev-only precision is 54/61/70/80/96% at 0.5/0.6/0.7/0.8/0.9 with Jev recall 100/100/94/90/63%. Per family at 0.7: self_admission 93%, realization 75%, correction 80%. By the pre-registered rule the recommendation is decide mode "regex OR Jev at 0.7" (0.8 is the reported alternative: 90% recall at 82% precision). It waits for the router decision. Durable: .plan/jev-stop-2026-09-25/ (PREREG, RESULTS, labels, key, every script). recall_rerank waits for much more data (user, 2026-09-25). 2026-09-25, from the rank-10 hook-lib sweep: skill_roster.py glues a bare "- name" listing line (a skill whose description the harness trimmed for budget) onto the PREVIOUS skill's description - 705 of 2111 real listings carry at least one corrupted entry - and drops those trimmed skills from the roster entirely (119 names), so the 7.16.0 installed-roster measurement and every choice-v1 row so far ran on corrupted option text; re-measure after the fix (rank-10 batch E). Also: the sweep's reviewer sessions wrote shadow rows too - exclude rows whose transcript_path contains "scratchpad-sweep" when evaluating. NEXT: let choice-v1 rows accumulate (only sessions on 7.21.0+ write them), run `classifier_eval.py report` on that group, and since a shadow row cannot say whether a pick was right, have a blind panel label a sample of them before putting decide mode to the user again; then put the stop_signal union at 0.7 to the user; recall_rerank last, ONE at a time
- [ ] (2026-09-01) [15] USER: a directive the user gave on 2026-09-01 only half reaches sessions - one half is pinned at the tree top and loads everywhere, the other sits at a level below projects/ and does not | size: 1 fact to re-level, 2 downward refs to resolve | open: the owning level and the two stranded ref targets are named in the memory store, not here - this repo is public | next: read the memory-store note, run ref_map.py on that slug, then either lift the two targets or demote those refs to prose, engine move, and require --check-tree TOTAL tree problems: 0
- [ ] (2026-08-27) [20] USER: the reviewer half of that same audit directive | size: unknown, sizing it is the first step | open: deferred by the user on 2026-08-28 in favour of the cheaper buckets | next: count what the script wave already covered, then re-scope
- [ ] (2026-08-31) [30] USER: "make it sort right" - zero-pad the numbered backup to .bak.001 | size: one function, two tests | open: deferred by the user on 2026-08-31, "we will come back later to it" | next: change next_backup_path() in plugins/bitranox/skills/compuse-toolbox/scripts/anchor_edit.py, and decide what happens past 999
- [ ] (2026-08-31) [40] USER: "once its commitet, delete those baks" - reap a backup once git can restore the file | size: one verb or one guard | open: deferred by the user on 2026-08-31 with item 3, and the shape needs deciding first, separate verb or automatic on the next edit | next: put the two shapes to the user
- [x] (2026-08-31) [50] USER: track EXECUTION-USER-REVIEW.md, or keep it out of the public repo | size: 1 file, 3225 lines | closed: 2026-08-31, the user chose to remove the file. It was never tracked, so no internal hostname reached git history.
- [ ] (2026-08-28) [60] FOUND: 206 unframed memory bodies | size: 206 | open: sweep never started | next: sample 10 and decide whether framing them is worth the pass
- [x] (2026-08-01) [70] FOUND: squash the memory store repo's history | size: 110 commits, past the ~50 threshold | open: blocked on a go from the user, the store has a private remote so squashing means force-pushing it | next: ask | closed: 2026-09-11 on the user's go ("squash"): 145 commits became one snapshot commit, force-pushed with a lease on the old sha. It waited until the store had seen no write for 15 minutes, because peer sessions were committing to it when the go came, and it ran by compare-and-swap (commit-tree of HEAD's tree, then update-ref against the old sha) instead of the orphan-branch method: a peer commit landing mid-swap makes the swap refuse rather than vanish, and a peer's uncommitted edits are never staged into the snapshot. The pre-squash history is kept in a git bundle that was proven by cloning it back
- [ ] (2026-08-28) [80] FOUND: 57 statusrot candidates | size: 57 | open: flagged by a scan, never triaged | next: adjudicate a sample against their source facts before believing the count
- [ ] (2026-09-01) [85] FOUND: 4 guard findings confirmed against the live tree and left unfixed | size: 4 | open: outside the scope the user chose on 2026-09-01, which was the 5 under-blocks only (shipped in 5.299.0) | next: validate-structured-files blocks valid JSON carrying a UTF-8 BOM; store-edit-guard's _read catches only OSError so a non-UTF-8 CLAUDE.local.md loses the DENY rather than the crash; subagent-model-gate downgrades DENY to WARN when the receipt store raises; skill-edit-guard's deny message calls a machine-wide 8h receipt a "session receipt". Detail in the GUARD SLICE section of TRIAGE.md, which survives only in hand-made ZFS snapshots - see item 10
- [ ] (2026-08-31) [90] FOUND: how a committed script names a compuse-toolbox jig's path | size: about 20 jigs | open: the installed plugin path is version-stamped and a glob plus tail -1 sorts lexicographically | next: decide a preamble rule or a vendoring step, with its own RED/GREEN; notes in plugins/bitranox/skills/compuse-toolbox/.skillwriter/checklist-20260831-anchor-edit.md
- [ ] (2026-08-30) [100] FOUND: 5 older queued contributions in contrib_queue | size: 5 | open: not drained | next: contrib_queue.py list, then check each against every shipped skill before writing it
- [ ] (2026-08-27) [105] FOUND: offer the soundtouch-decloud skill (its own repo, bitranox/soundtouch-decloud) upstream to the gesellix/bose-soundtouch project as an ISSUE, not a PR - their CONTRIBUTING asks for small PRs and this is 18 non-Go files into a repo with no skills/ dir | size: one issue, body needs rewriting | open: carried out of the kpx01 handover of 2026-08-27, which is the only record of it; it appears in no backlog, so whether it was ever done is UNKNOWN. The drafted body it referred to lived in a session scratchpad that is long gone | next: check the upstream issue tracker for an existing one before writing anything, and confirm with the user before opening anything public
- [x] (2026-09-23) [112] FOUND: anchor_edit's nudge rule is spelled `sed -i`, but the dominant real-world spelling of its chore is a Python heredoc doing an exact-text replace, which no rule sees | closed: 2026-09-23 in 7.9.0, and NOT as the line first proposed. A plain rule cannot reach it: extract_text blanks the whole heredoc body by design, so the fix gives the body the `_ANY_TOOL_RULES` pass (the hook's own real-when-authored set, the same reading Write and Edit content already gets) with the command reading keeping precedence. Measured over 84,828 recorded Bash calls: command reading 20.84%, this pass adds 5.61% where it was silent (4,757 calls, 4,386 anchor_edit); 12 firings sampled across the corpus and adjudicated against their text, 11 the chore exactly and 1 a correct mdwrap - a sample shows shapes, not a proportion, so that is not a precision figure for all 4,757. shell_text gained heredoc_bodies() as the exact inverse of strip_heredoc_bodies(), both from one scanner | size: 1 rule plus its firing-rate measurement, and a re-check of whether `gate` and `claim_check` should keep claiming those commands | open: found 2026-09-23 by jig_probe over 60 sampled calls. In 5 of the 11 rows where both channels spoke, the rules said `gate` or `claim_check` - matching incidentally on a `2>&1 | tail` or a `grep -c` in the same command - while Jev said `anchor_edit` and was right on the text. So the chore is not merely unrouted, it is currently routed to the WRONG jig | next: NOT a plain rule - that premise was disproven 2026-09-23 before implementing. `extract_text` BLANKS heredoc bodies on purpose (so prose naming a chore does not trip the guard watching for it), so the whole Python program is invisible and `gate` matched the surviving `pytest ... 2>&1 | tail`. A regex cannot reach the chore at all. The shape that fits the hook's own distinction: a heredoc body is AUTHORED text, like Write/Edit content, so give it the `_ANY_TOOL_RULES` pass (the rules already marked real-when-authored) while keeping it blanked for the command rules, then measure the rate with guard_replay and adjudicate the firings before shipping
- [x] (2026-09-22) [115] FOUND: a harness turn shape that `transcript_turns.NOT_TYPED_PREFIXES` does not cover, so it is still scored as if a person typed it | closed: 2026-09-23 in 7.10.0, and the enumeration its `next:` asked for was done first rather than a rule being added for the one known shape. Over the corpus, 112 of 1,409 turns these readers called typed were the harness: 82 `[Request interrupted`, 14 the counted shape, 16 opening with a tag. The tag group SPLIT in two and that is why no blanket rule was written - `<bash-input` and `<bash-stdout` (12) are the shell escape and its output, while `<pasted_content` (4) IS the person, so it stays scored and a test says so. Answer to the open question: BOTH, but for different reasons - the prefix tuple takes the three literal shapes, and a new `NOT_TYPED_PATTERNS` takes only the one that opens with a COUNT, since a prefix structurally cannot reach it. Both are read through one `transcript_turns.looks_typed`, so a shape added for the Stop gate cannot go missing from the router. Verified as a firing-SET diff, not a count: 1,409 to 1,301, 108 removed and 0 added | size: 1 shape, needs a regex rather than a prefix | open: found while adjudicating the 7.6.0 corpus replay. "2 background agents were stopped by the user: <quoted briefs>" is harness-generated, and it matched meta-adopting-external-skills plus meta-audit-local-skills-and-hooks from the paths inside the quoted briefs. It STARTS WITH A COUNT, so the prefix tuple cannot match it; prose stripping already neutralises this instance, but the shape is unregistered and the registry is what the Stop gate shares | next: decide whether NOT_TYPED_PREFIXES grows a regex arm or a second tuple of patterns, then enumerate the other counted harness shapes before adding one
- [x] (2026-09-22) [117] FOUND: no jig replays typed PROMPTS through a predicate, so measuring a prompt-side guard means hand-rolling the corpus walk | closed: 2026-09-23 in 7.10.0 as its OWN jig, `compuse-toolbox/scripts/corpus_prompts.py`, not as a `--field prompt` mode on guard_replay. The two answer different questions and bolting them together would have distorted the one that works: guard_replay scores a guard by rate AND precision, where precision means a gate refused the call, and a prompt has no gate for that column to read. It ships `diff_predicates`, which compares two predicates by their firing SETS because two rules can fire the same number of times on different prompts. Which records count was MEASURED rather than assumed: across 300 transcripts, 14,884 `type: user` records are 13,790 tool results, 890 strings and 203 lists of text, and `origin` appears on 245, which is why nothing filters on it. Its toolbox-nudge exemption carries measured evidence over 84,968 Bash calls rather than an assertion. First use closed rank 115 | size: 1 jig, or a flag on guard_replay | open: guard_replay replays tool_use payloads (Bash commands and their cwd) and there is no prompt equivalent, so the 7.6.0 measurement was a throwaway script in a session scratchpad and is gone. A prompt replay needs the corpus walk, the typed-vs-machine split, and a firing-SET diff between two predicates rather than a count | next: price it as a --field prompt mode on guard_replay against a second jig, then measure it on a known-positive and a known-negative as that skill requires
- [ ] (2026-08-31) [110] FOUND: consolidate transcript_index against the shipped jsonl_grep and transcript_tail | size: 3 tools, 1 overlap | open: not started | next: diff what each answers before merging anything
- [ ] (2026-08-31) [120] FOUND: the tree-top fact on restoring from a copy should gain the automated form of its rule | size: 1 fact body | open: was waiting on a dream that has since finished | next: memory_engine.py add, upserting at the level that owns the pointer
- [ ] (2026-08-31) [130] FOUND: a standing memory rule still tells every session to write EXECUTION-USER-REVIEW.md, so the removed file comes back | size: 1 memory fact, feedback-log-autonomous-decisions-to-execution-user-review-md-on-auto-tasks | open: the file was removed on 2026-08-31 but the rule that creates it was not changed | next: decide whether to retire the rule or point it at a location outside the public repo, then update the fact
- [x] (2026-08-31) [65] FOUND: contrib_context can blow the whole SessionStart essentials budget on its own | size: 11481 bytes measured at the queue's 100-entry cap, against a 3300 ceiling | open: pre-existing and unbounded, it formats every queued entry with no budget of its own; over the ceiling the harness persists the WHOLE essentials block and injects a ~2KB preview, so every other block goes with it | next: give it the same remaining-budget treatment the backlog block now has, and a compact fallback | closed: 2026-09-12 in 6.12.0. The backlog is sized first with the contribution count line held back, the listing gets what is left, and it falls back to the count line when not one entry fits; a test fills the queue to 100 in a dream room and requires the whole essentials block within 3300 bytes with both blocks present. Fixing it found the backlog block's own overrun: its '... and N more' line was appended after the budget was spent (984 bytes in a 982-byte budget), and both listings now reserve that line before admitting an entry
- [ ] (2026-09-01) [95] FOUND: meta-claude-hooks is no longer exhaustive - upstream added PreModelSwitch and PostModelSwitch (31 -> 33 events) plus env var CLAUDE_CODE_SUBPROCESS_ENV_SCRUB | size: 2 events, 1 env var, nothing removed | open: hookdoc_stamp.py check returned STRUCTURAL on 2026-09-01 and the skill still says "31 hook events" | next: document both events in references/events.md and the env var in references/authoring.md, then hookdoc_stamp.py stamp --write (it refuses while coverage has gaps)
- [ ] (2026-09-04) [125] FOUND: re-measure the instrumentation share of work-project sessions two weeks after 6.6.0-6.9.1 shipped | size: one corpus run, 5 tables | open: the classifier is a queued compuse-toolbox jig (contrib_queue), not shipped; the baseline is in CHANGELOG 6.6.0-6.9.0 and this file's git history | next: on or after 2026-09-18 run the classifier over ~/.claude/projects, expect the hook-driven share of work-project minutes under a fifth and the decision-review row near zero; if not, the levers missed the mechanism
- [ ] (2026-08-31) [135] FOUND: check whether the prose reconcile step actually holds | size: the next few handovers | open: the carry-forward rule is prose and nothing verifies it, which is the shape that caused the original drift; left as prose deliberately because the read side is now covered | next: after a few real handovers, diff OPEN-WORK.md against what each outgoing handover carried, and look for an item that lost its size or its line
- [x] (2026-08-21) [140] FOUND: backfill the 153 older CHANGELOG entries | size: 153 | closed: 2026-08-31, the user declared a completeness floor at 5.266.2 instead, and check_changelog_current_version now stops the class recurring. Not a backlog item; recorded so it is not rediscovered a third time.
- [x] (2026-09-12) [75] FOUND: the same --set provenance defect the layered-config skill now warns about sits in the CLI template's own config overrides, and in the repos derived from it | size: 1 template fix plus a distribution; the derived-repo count came from the queue entry and is NOT re-verified here, so size it before scheduling | open: the skill teaching the fix shipped in 6.11.0, but the repo inventory was deliberately kept out of a public skill, which left the template itself with no tracker | next: read the template's override application against the shipped rule - rebuild provenance for the overridden keys as layer cli with no path, keying on section plus key_path - fix it there, then run its distribution | closed: 2026-09-12 in bitranox_template_py_cli 1.7.2 - the override path rebuilds provenance, naming layer cli with no path for exactly the keys --set supplied, keyed on section plus key_path because ConfigOverride.key_path excludes the section. Held by a test seen failing first and a control asserting an untouched key keeps its layer, plus a doctest; that repo's full gate passed. Correction to this line's plan: there is no distribution to run. bitranox_template_py_cli is COPIED by rename.sh, not distributed, so the derived repos do not pick this up automatically and their count is still unsized - carried as rank 76
- [ ] (2026-09-23) [14] USER: "go ahead" (2026-09-23) to the guard proposed when the invented-identifier rule reached recurrence 3 | size: one PreToolUse hook plus sibling tests, plus a corpus replay to measure its firing rate before it ships | open: prose has failed three times - a sha (2026-08-23), a PID (2026-09-04), and padding a short sha to 40 chars (2026-09-23), the last one in a session whose own inherited handover already stated the rule. Approved but NOT started: raised at ~530k context, so it was ranked rather than begun | next: write a PreToolUse Bash hook emitting NON-BLOCKING additionalContext when a command carries a bare 40-hex literal with no command substitution and no rev-parse in that same command; keep it non-blocking because a sha from a CI URL or a paste must stay possible. Measure the firing rate by replaying the real Bash corpus BEFORE shipping (unit tests prove it fires, never that it is quiet), and adjudicate every firing against its source transcript. Fact: feedback-a-sha-you-did-not-derive-is-one-you-may-have-invented
- [x] (2026-09-23) [78] FOUND: a test in the meta-self-improve suite leaks a fake skill name into a module-level registry that toolbox-nudge reads, so test_still_silent_for_a_tool_that_exists_nowhere FAILS when the skills suite runs before the hooks suite | size: one fixture or one teardown, plus a test pinning the isolation | open: invisible to CI and to repo-gate, which walk the tree in the other order (hooks before skills), so the suite is green everywhere it is actually run; found only by running the two dirs explicitly in the reverse order | next: reproduce with `pytest plugins/bitranox/skills/meta-self-improve/tests/ plugins/bitranox/hooks/tests/test_toolbox_nudge.py` (passes when that file runs alone), find which catalogue test writes the name test_catalogue_takes_each_jig_0 into the shared registry, and give it a teardown rather than reordering anything | closed: 2026-09-24 in 7.15.1, and the diagnosis above was wrong - there is no registry. The test faked the shipped scripts dir as tmp_path/"nowhere", and the resolver's sibling-skill search runs at _shipped_dir().parent.parent, which is pytest's SHARED temp root, where test_jig_probe's own tmp_path (test_catalogue_takes_each_jig_0/scripts/procsig.py) made the tool exist. Reproduced with just those two tests in order; both tests that fake the shipped dir now put it inside a skills tree they own (_no_shipped_tree). Also no longer invisible: repo-gate --ci hit it in whole-repo order on 2026-09-24
- [ ] (2026-09-24) [150] FOUND: the main checkout has TODO-JEV.md STAGED as a new file (git status AM) and sits 32 commits behind origin/master | size: 1 file, 1 fast-forward | open: first seen here; TODO-JEV.md is NOT gitignored (git check-ignore answers no), its Progress section stops at 7.1.0 while the live Jev record is rank 12 above, and whoever staged it is unknown - a peer session may own it. 2026-09-25: no live Claude session had its cwd in the main checkout; the user chose "commit as design doc", so it is committed as docs/jev-design.md with its stale design statements corrected and Progress replaced by a pointer to rank 12. Left: the main checkout still holds the staged TODO-JEV.md, and a worktree-isolated session cannot run git there | next: from a session in the main checkout, `git rm --cached TODO-JEV.md`, delete the file (its content is docs/jev-design.md), then fast-forward master
- [ ] (2026-09-24) [160] FOUND: two finished worktrees still on disk, `.claude/worktrees/jev-roster` and `.claude/worktrees/nudge-recall-fix`, both fully merged into master | size: 2 worktrees | open: first seen here, carried from the 2026-09-24 handover; jev-roster is where the 7.16.0-7.18.0 sessions ran, so it cannot be removed from inside itself. Its gitignored .plan/jev-stop-2026-09-25/ is already copied to the main checkout's .plan/ (diff -r identical, 2026-09-25) | next: from the main checkout, run wtclean on both (bitranox:git-worktrees), after confirming `git log origin/master..<branch>` is empty for each
- [ ] (2026-09-24) [82] FOUND: toolbox-nudge's git_state rule only matches a repo loop that runs `status`, so a `for r in ...; do git -C "$r" log/rev-parse/push ...` sweep goes un-nudged | size: one regex plus tests, and a replay | open: deliberately left out of 7.15.1, which fixed pushcheck and backstop from the same recall measurement - of 65 silent candidates about half loop over REPOS (the chore) and half loop over refs, commits or files inside ONE repo (not the chore), so a naive widening would fire on the second half | next: write the rule on the loop VARIABLE being a git -C target rather than on the subcommand, replay old against new over the Bash corpus and read every changed verdict before shipping, as 7.15.1 did
- [ ] (2026-09-12) [76] FOUND: the repos derived from bitranox_template_py_cli may carry the same --set provenance defect, and that template is COPIED by rename.sh rather than distributed, so none of them picks the fix up on its own | size: unknown - the original queue entry said about 31 repos and that figure is NOT verified here, so sizing it is the first action | open: raised 2026-09-12 while closing rank 75, which wrongly assumed a distribution step existed | next: grep the derived repos for an apply_overrides that returns config.with_overrides(...) directly, count them, then decide bulk fix versus fix-on-touch
- [x] (2026-09-24) [145] FOUND: classifier-shadow.jsonl has no size bound | size: one rotation rule plus tests | open: first seen here; 13 MB after 2.2 days at about 22 KB a row (full redacted state per row), nothing rotates or trims it, and classifier_eval.py reads it whole | next: decide keep-N-days vs size-capped rotation with the old file kept beside it, and make `report` read the rotated file too | closed: 7.22.0 - one file per UTC day, pruned on every append to the last 30 days and then oldest-first to 200 MB, never the current day (user chose 30 days / 200 MB on 2026-09-25 after 3-11 MB/day was measured); the undated file stays readable and ages out by its last write; `report` reads the whole audit directory
